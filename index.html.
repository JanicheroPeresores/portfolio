<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>JPeresores Portfolio</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen,
        Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
      background: #fafafa;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .iphone {
      width: 380px;
      height: 750px;
      background: white;
      border-radius: 40px;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
      overflow: hidden;
      border: 10px solid black;
      display: flex;
      flex-direction: column;
    }

    header {
      text-align: center;
      padding: 20px;
      border-bottom: 1px solid #ddd;
    }

    #profile-img {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      object-fit: cover;
      margin: 15px 0;
      border: 4px solid #007aff;
    }

    h1 {
      margin: 10px 0 5px;
      font-size: 24px;
      font-weight: 700;
    }

    #course-year {
      font-size: 14px;
      color: #555;
    }

    section {
      padding: 20px;
      overflow-y: auto;
      flex: 1;
    }

    h2 {
      color: #007aff;
      margin-bottom: 10px;
    }

    ul {
      list-style: none;
      padding: 0;
    }

    li {
      background: #f0f0f5;
      padding: 12px;
      margin: 8px 0;
      border-radius: 12px;
    }

    #theme-toggle {
      padding: 10px 20px;
      border: none;
      background: #007aff;
      color: white;
      border-radius: 12px;
      cursor: pointer;
      font-size: 14px;
    }

    .dark {
      background: #1c1c1e;
      color: white;
    }

    .dark section li {
      background: #2c2c2e;
    }

    .dark #theme-toggle {
      background: #0a84ff;
    }
  </style>
</head>
<body>
  <div class="iphone" id="phone">
    <header>
      <h1 id="name">Janichero Peresores</h1>
      <img src=""D:\CLASS\NEW\AKI.jpg"" alt="Profile Picture" id="profile-img" />
      <p id="IS-2ND Year"></p>
      <!-- Replace 'your-image.jpg' with the file name of the photo you uploaded -->
      <button id="theme-toggle">Switch Theme</button>
    </header>

    <section id="achievements">
      <h2>Achievements / Certifications</h2>
      <ul>
        <li>Darts Champion 2024</li>
        <li>Top 4 3man-Darts</li>
        <li>Regional Runner Up Badminton</li>
      </ul>
    </section>

    <section id="projects">
      <h2>Previous Projects</h2>
      <ul>
        <li>Engine CC Calculator</li>
        <li>Home-wage Calculator</li>
        <li>Smartphone UI Design</li>
      </ul>
    </section>
  </div>

  <script>
    const toggleBtn = document.getElementById("theme-toggle");
    const phone = document.getElementById("phone");

    toggleBtn.addEventListener("click", () => {
      phone.classList.toggle("dark");
    });
  </script>
</body>
</html>
